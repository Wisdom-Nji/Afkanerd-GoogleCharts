
class Graphs {
	this.type = ""
	constructor( DOMLocation ) {}

	addSlicer( cl_slicers ) // cl_% = class

	setTitle( title )

	setWidth( width )

	setHeight( height )

	setLengendPosition( position )

	setOption( option )

	render()
}
